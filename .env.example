TYPESAFE_API_KEY=your-key-here
# Optional. Per-IP budget for POST /api/ask; defaults shown.
# ASK_RATE_LIMIT=10
# ASK_RATE_WINDOW_SECONDS=60
# Set to 1 behind a reverse proxy so the limit keys on X-Forwarded-For.
# TRUST_PROXY=1
